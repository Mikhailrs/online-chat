<a class="block hover:bg-gray-50 py-2 shadow overflow-hidden border-b border-gray-200 sm:rounded-lg my-4 py-2 mb-2">
  <div class="px-4 py-4 sm:px-6">
    <div class="flex items-center justify-between">
      <p class="font-thin text-black-700 truncate">
        <%= notification.params[:message].body %>
      </p>
      <div class="ml-2 flex-shrink-0 flex">
        <p class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
          <%= Room.find(notification.params[:message].room_id).title %>
        </p>
      </div>
    </div>
    <div class="mt-2 sm:flex sm:justify-between">
      <div class="sm:flex">
        <p class="flex items-center text-sm font-light text-gray-500">
              Received on <%= notification.created_at.to_date %>
        </p>
      </div>
    </div>
  </div>
</a>